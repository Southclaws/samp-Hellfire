#include <a_samp>	// All SA:MP native functions
#include <SIF/SIF>	// Interactivity framework + all dependencies

/*
	Filterscript

		A separate script from the main gamemode script, all callbacks are
		called here first before being called in the main gamemode script.

		Filterscripts can be loaded and unloaded at any point while the server
		is running. Which makes them useful for testing stuff.
*/

public OnFilterScriptInit() // Called when the script is loaded
{

}
